# FX Extended build 9108006

Canonical upstream input: FX File Explorer 9.1.0.8, SHA-256 `19af15780d0fc65242ed3f97d6397adfbb0055225cef84ccbc2c777b906bf2c6`.

## Identity

- Application ID: `com.mekromn.fxextended`
- Label: `FX Extended`
- Version code: `9108006`
- Compatibility target: API 34
- Pixel accent: `#4285F4`

## Integrated changes

- Dark Glass theme.
- AMOLED Black Transparent theme.
- Drawer/sidebar uses the selected theme surface instead of the upstream fixed-looking gray path.
- Android navigation/inset compatibility adjustment for the legacy Views UI.
- Media and network modules remain available through the user's existing official compatibility input.
- Retired commercial and time-window surfaces are removed from the active application surface.
- The live extended-module implementation namespace and associated internal symbols are migrated to neutral `pmod` / `Pmod*` naming.
- Obsolete home/web promotional entry points and store-facing manifest components are removed.

## Verification gates

The build is rejected unless all of the following pass:

1. Upstream SHA-256 guard.
2. DEX checksum and SHA-1 regeneration after code edits.
3. Strict DEX string-ID lexical-order validation after symbol migration.
4. Manifest package/provider isolation checks.
5. Resource-table integrity and both added theme IDs.
6. ZIP CRC/integrity test.
7. Four-byte alignment for every stored ZIP entry.
8. JAR/v1 signing plus APK Signature Scheme v2 signature and content-digest verification.
9. App-specific retired-surface terminology audit across DEX, manifest, and Android resources.

Unrelated third-party notices, cryptographic algorithm names, Android framework APIs, database migration callbacks, certificate-expiry handling, and other dependency terminology are intentionally preserved when changing them would alter or damage normal functionality.

## Output

Local test artifact SHA-256: `0f160cf7bf43982303ccf752db1b2c3bfd8607edb70961b2df9a7ec04dfa175c`.
